name: PHP Auto-Fix & Quality Check

on:
  push:
    branches:
      - '**'
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  # 🔧 Auto-fix coding standards (only once, PHP 8.1)
  phpcbf-fix:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout code (allow pushing)
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0
          persist-credentials: true

      # 2️⃣ Setup PHP (single version for auto-fix)
      - name: Setup PHP 8.3
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.3
          coverage: none

      # 3️⃣ Install PHPCS
      - name: Install PHPCS
        run: |
          composer global require squizlabs/php_codesniffer
          echo "$HOME/.config/composer/vendor/bin" >> $GITHUB_PATH

      # 4️⃣ Run PHPCBF (auto-fix issues). Fail on phpcbf error. If fixes applied, commit, re-check and push.
      - name: Run PHPCBF and commit fixes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -o pipefail

          echo "Running PHPCBF (first pass)..."
          phpcbf --standard=PSR12 phpcbf/ 2>&1 | tee phpcbf-output.txt
          EXIT_CODE=${PIPESTATUS[0]}

          if [ "$EXIT_CODE" -ne 0 ]; then
            echo "PHPCBF exited with code $EXIT_CODE. See output:"
            cat phpcbf-output.txt || true
            exit $EXIT_CODE
          fi

          if ! git diff --quiet; then
            echo "PHPCBF modified files — committing fixes."
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add -A
            git commit -m "PHPCBF: auto-fix coding standards"

            echo "Re-running PHPCBF on committed state (verification)..."
            phpcbf --standard=PSR12 phpcbf/ 2>&1 | tee phpcbf-output-2.txt
            EXIT_CODE2=${PIPESTATUS[0]}

            if [ "$EXIT_CODE2" -ne 0 ]; then
              echo "PHPCBF verification run failed with code $EXIT_CODE2. See output:"
              cat phpcbf-output-2.txt || true
              exit $EXIT_CODE2
            fi

            echo "Pushing fixes back to branch."
            BRANCH="${GITHUB_HEAD_REF:-${GITHUB_REF#refs/heads/}}"
            git push https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git HEAD:$BRANCH --force-with-lease
          else
            echo "No changes from PHPCBF."